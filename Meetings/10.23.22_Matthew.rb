# linked lists are more useful for interview coding problems
# tree data structure - fibonacci recursive image, branching out like a tree data structure
  # every node knows the right and left children of itself
  # example of a linked list data structure
  # using a little bit of memory to find them
# how a random collection is implemented, libraries handle it for us
  # hash is a standard library data structure
  # not a standard library cuz you don't have to import it
    # stuff that comes with the language, don't have to install it
  # anything listed in the Ruby collections
# is there another way that Ruby uses set 
  # hashes without any values